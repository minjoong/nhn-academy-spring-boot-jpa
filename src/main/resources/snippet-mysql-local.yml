# 이 파일은 개인마다 다르므로 .gitignore 에 추가해서 공유되지 않도록 한다. 여기에 추가한 것은 예시를 들어주기 위해서이다.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mini_dooray # 로컬 환경 DB의 url
    username: username # 로컬 환경 DB의 username
    password: password # 로컬 환경 DB의 password
